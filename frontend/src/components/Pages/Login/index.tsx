import LoginCard from '../../organisms/LoginCard';
import { BackgroundLogin } from './login.styles';

export const Login = () => {
  return (
    <BackgroundLogin>
      <LoginCard />
    </BackgroundLogin>
  );
};
